#include <stdio.h>
#include {stlib.h>



int main(int argc, char** argv)
{

/*
 *
 * First read arguments
 *
 * check if directory/file exists
 *
 * if not: print error
 *
 * else: continue
 *
 * call method to create file
 *
 * now we recursively go through
 *
 * single file is base case
 *
 * For single file: 
 *
 * call tokenizer method
 *
 * Upon finding separate word: store it into trie
 *
 * Run through until last word
 */
}
